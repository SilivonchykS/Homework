package classwork.classwork_38.example_01;

public class Lecture {
   /* private String topic;
    private double duration;//dlitelnost
    private Student[] students;
    private int studentAmount;

    public String getTopic() {
        return topic;
    }

    public double getDuration() {
        return duration;
    }

    public void setTopic(String topic) {
        this.topic = topic;
    }

    public void setDuration(double duration) {
        this.duration = duration;
    }

    public void setStudentAmount(int studentAmount) {
        this.studentAmount = studentAmount;
    }

    public Lecture(String topic, double duration, int capacity) {
        this.topic = topic;
        this.duration = duration;
        students=new Student[capacity];
        studentAmount=0;



    }
    public void addStudent(Student student){
        if (studentAmount== students.length){
            System.out.println("Keine Frei Pleatzen!");
            return;
        }
        students[studentAmount]=student;
        studentAmount++;
    }
    public void removeStudent(Student student){
        boolean found=false;
        for (int i=0;i<studentAmount;i++){
            if (!found){
                if (student=students[i]){
                    found=true;
                }
            }
            if (found){
                students[i]=students[i+1];
            }
        }
        if (found){
            studentAmount--;
        }
    }
    public void beginLecture(){
        System.out.println("Anfangin Lecture von Tema" + topic);
        System.out.println("Studenten in Lecture:" + studentAmount);
        System.out.println("Hier Studenten:");
        for (int i=0;i<studentAmount;i++){
            System.out.println(students[i].getName()+ " ,");
            students[i].getHoursStudied()+duration);
        }
        System.out.println("(Lecture geht");
        System.out.println("Lecture finisched! Zeit der Lecture:" + duration + "Menchen");*/
    }

